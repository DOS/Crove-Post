name: Build & Deploy Crove Containers

on:
  push:
    branches:
      - main
      - dev
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.claude/**'
      - 'reports/**'
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Target Environment'
        required: true
        default: 'beta'
        type: choice
        options:
          - beta
          - prod

jobs:
  validate-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js & PNPM
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup PNPM
        uses: pnpm/action-setup@v4

      - name: Install Root Dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Verify Branding Guard
        run: npx tsx scripts/branding-guard.ts

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine Image Tags
        id: tags
        run: |
          REPO_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.target_env }}"
          elif [ "${{ github.ref_name }}" = "main" ]; then
            TAG="latest"
          else
            TAG="beta"
          fi
          echo "owner=${REPO_OWNER}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      # 1. Build Crove Landing Page Container (@crove/web)
      - name: Build and Push Crove Web Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/web/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.tags.outputs.owner }}/crove-web:${{ steps.tags.outputs.tag }}
            ghcr.io/${{ steps.tags.outputs.owner }}/crove-web:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 2. Build Postiz Core App Container
      - name: Build and Push Postiz App Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.dev
          push: true
          tags: |
            ghcr.io/${{ steps.tags.outputs.owner }}/crove-app:${{ steps.tags.outputs.tag }}
            ghcr.io/${{ steps.tags.outputs.owner }}/crove-app:${{ github.sha }}
            ghcr.io/${{ steps.tags.outputs.owner }}/postiz-app:${{ steps.tags.outputs.tag }}
            ghcr.io/${{ steps.tags.outputs.owner }}/postiz-app:${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_VERSION=${{ steps.tags.outputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-to-gcp:
    needs: validate-and-build
    if: github.repository_owner == 'gitroomhq' || github.repository_owner == 'MetaDOS'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Server Sync (Optional Webhook/SSH)
        run: |
          echo "Build and container push completed successfully for tag: ${{ needs.validate-and-build.outputs.tag || github.ref_name }}."
          echo "Ready for deployment on crove-server."
